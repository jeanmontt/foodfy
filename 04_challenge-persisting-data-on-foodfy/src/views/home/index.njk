{% extends "home/layout.njk" %}

{% block head %}
  <link rel="stylesheet" href="/styles/home/index.css">

  <title>Foodfy</title>
{% endblock head %}

{% block content %}
  <section id="introduction">
    <div class="info">
      <h1>As melhores receitas</h1>
      <p>
        Aprenda a construir os melhores pratos com receitas criadas por profissionais do mundo inteiro.
      </p>
    </div>

    <div class="image">
      <img src="./assets/chef.png" alt="Chef Foodfy">
    </div>
  </section>

  <section id="most_accessed">
    <h1>Mais acessadas</h1>

    <div class="cards">
      <div class="card" id="1">
        <img src="{{ data[0].image }}" alt="{{ data[0].title }}">
        <h1>{{ data[0].title }}</h1>
        <p>por {{ data[0].author }}</p>
      </div>

      <div class="card" id="2">
        <img src="{{ data[1].image }}" alt="{{ data[1].title }}">
        <h1>{{ data[1].title }}</h1>
        <p>por {{ data[1].author }}</p>
      </div>

      <div class="card" id="3">
        <img src="{{ data[2].image }}" alt="{{ data[2].title }}">
        <h1>{{ data[2].title }}</h1>
        <p>por {{ data[2].author }}</p>
      </div>

      <div class="card" id="4">
        <img src="{{ data[3].image }}" alt="{{ data[3].title }}">
        <h1>{{ data[3].title }}</h1>
        <p>por {{ data[3].author }}</p>
      </div>

      <div class="card" id="5">
        <img src="{{ data[4].image }}" alt="{{ data[4].title }}">
        <h1>{{ data[4].title }}</h1>
        <p>por {{ data[4].author }}</p>
      </div>

      <div class="card" id="6">
        <img src="{{ data[5].image }}" alt="{{ data[5].title }}">
        <h1>{{ data[5].title }}</h1>
        <p>por {{ data[5].author }}</p>
      </div>
    </div>
  </section>

  {% block scripts %}
    <script src="/scripts/scripts.js"></script>
  {% endblock scripts %}
{% endblock content %}